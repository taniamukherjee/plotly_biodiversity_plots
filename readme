Use Flask to design an API for a biology dataset and to serve the HTML and JavaScript required for  dashboard page. 
used using the sqlite database file and SQLAlchemy inside  Flask application code, 
 
